/* eslint-disable */
import Layout from './Layout';

export default {
    title: "Layout",
};

export const Default = () => <Layout children={undefined} />;

Default.story = {
    name: 'default',
};
